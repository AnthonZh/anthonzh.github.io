//Easter Egg for Rossini

function changeInner(id, val) {
    const element = document.getElementById(id);
    element.innerHTML = val;
}

//Plans to add other functions unlikely - not enough time